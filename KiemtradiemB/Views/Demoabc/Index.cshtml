@model IEnumerable<KiemtradiemB.Demoabc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fullname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fullname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diem)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PesonId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PesonId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PesonId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
